<Page x:Class="EfiGuardUI.Views.OperationsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d"
      Background="Transparent">

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="32,24" MaxWidth="900" HorizontalAlignment="Center">
            <!-- Hero -->
            <StackPanel HorizontalAlignment="Center" Margin="0,0,0,32">
                <Border Background="#331a00" BorderBrush="#663300" BorderThickness="1" CornerRadius="16"
                        Padding="10,4" HorizontalAlignment="Center" Margin="0,0,0,16">
                    <StackPanel Orientation="Horizontal">
                        <Ellipse Width="6" Height="6" Fill="#ff9f0a" Margin="0,0,8,0"/>
                        <TextBlock x:Name="OpsBadge" Text="系统操作 / System Ops" Foreground="#ff9f0a" FontSize="11" FontWeight="SemiBold"/>
                    </StackPanel>
                </Border>
                <TextBlock x:Name="OpsTitle" Text="操作中心" FontSize="40" FontWeight="Bold" Foreground="#f5f5f7" HorizontalAlignment="Center"/>
                <TextBlock x:Name="OpsSubtitle" Text="管理 VBS 与 EfiGuard 启动配置" FontSize="16" Foreground="#6e6e73"
                           HorizontalAlignment="Center" Margin="0,8,0,0"/>
            </StackPanel>

            <!-- Operation Cards -->
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Disable VBS Card -->
                <Border Grid.Column="0" Background="#1d1d1f" BorderBrush="#2a2a2c" BorderThickness="1"
                        CornerRadius="16" Padding="28" Margin="8">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,16">
                            <Border Width="48" Height="48" Background="#111113" CornerRadius="12" Margin="0,0,14,0">
                                <TextBlock Text="&#x1F6E1;" FontSize="22" Foreground="{StaticResource TextPrimaryBrush}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <StackPanel>
                                <TextBlock x:Name="VbsTitle" Text="关闭 VBS" FontSize="20" FontWeight="Bold" Foreground="#f5f5f7"/>
                                <Border Background="#331a1a" CornerRadius="10" Padding="6,2" HorizontalAlignment="Left" Margin="0,4,0,0">
                                    <TextBlock x:Name="VbsTag" Text="高风险 / DANGER" FontSize="10" FontWeight="Bold" Foreground="#ff453a"/>
                                </Border>
                            </StackPanel>
                        </StackPanel>
                        <TextBlock x:Name="VbsDesc" Text="关闭基于虚拟化的安全功能和所有 Hyper-V 相关组件。将修改 Windows 可选功能和 BCD 配置，重启后生效。"
                                   Foreground="#6e6e73" FontSize="13" TextWrapping="Wrap" Margin="0,0,0,20" LineHeight="20"/>
                        <Button x:Name="BtnDisableVbs" Style="{StaticResource ActionButtonDanger}" Click="BtnDisableVbs_Click">
                            <TextBlock x:Name="BtnDisableVbsText" Text="立即关闭 VBS"/>
                        </Button>
                    </StackPanel>
                </Border>

                <!-- Install EfiGuard Card -->
                <Border Grid.Column="1" Background="#1d1d1f" BorderBrush="#2a2a2c" BorderThickness="1"
                        CornerRadius="16" Padding="28" Margin="8">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,16">
                            <Border Width="48" Height="48" Background="#111113" CornerRadius="12" Margin="0,0,14,0">
                                <TextBlock Text="&#x26A1;" FontSize="22" Foreground="{StaticResource WarnBrush}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <StackPanel>
                                <TextBlock x:Name="EfiTitle" Text="安装 EfiGuard" FontSize="20" FontWeight="Bold" Foreground="#f5f5f7"/>
                                <Border Background="#0d1f33" CornerRadius="10" Padding="6,2" HorizontalAlignment="Left" Margin="0,4,0,0">
                                    <TextBlock x:Name="EfiTag" Text="高级 / ADVANCED" FontSize="10" FontWeight="Bold" Foreground="#0066cc"/>
                                </Border>
                            </StackPanel>
                        </StackPanel>
                        <TextBlock x:Name="EfiDesc" Text="将预打包的 EfiGuard v1.3 部署到 EFI 系统分区并创建 BCD 启动项。EfiGuard 会在启动时禁用 PatchGuard 和驱动签名强制 (DSE)。"
                                   Foreground="#6e6e73" FontSize="13" TextWrapping="Wrap" Margin="0,0,0,20" LineHeight="20"/>
                        <Button x:Name="BtnInstallEfi" Style="{StaticResource ActionButtonPrimary}" Click="BtnInstallEfi_Click">
                            <TextBlock x:Name="BtnInstallEfiText" Text="一键安装到 ESP"/>
                        </Button>
                        <TextBlock x:Name="EfiStatusText" Text="" Foreground="#86868b" FontSize="12" Margin="0,12,0,0"/>
                    </StackPanel>
                </Border>
            </Grid>

            <!-- Warning Panel -->
            <Border x:Name="WarningPanel" Background="#1a1400" BorderBrush="#332a00" BorderThickness="1"
                    CornerRadius="12" Padding="20" Margin="8,16,8,0">
                <StackPanel Orientation="Horizontal">
                    <Border Width="36" Height="36" Background="#2a2000" CornerRadius="18" Margin="0,0,14,0">
                        <TextBlock Text="!" Foreground="#ff9f0a" FontSize="18" FontWeight="Bold"
                                   HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <StackPanel>
                        <TextBlock x:Name="WarnTitle" Text="重要提示 / Important Notice" Foreground="#ff9f0a" FontSize="14" FontWeight="Bold" Margin="0,0,0,6"/>
                        <TextBlock x:Name="WarnHvci" Text="EfiGuard 无法禁用 HVCI（内存完整性）。如果 HVCI 处于启用状态，EfiGuard 的 DSE 绕过将无效。"
                                   Foreground="#6e6e73" FontSize="12" TextWrapping="Wrap" LineHeight="18"/>
                        <TextBlock x:Name="WarnReboot" Text="所有系统级修改都需要重启计算机才能生效。"
                                   Foreground="#6e6e73" FontSize="12" TextWrapping="Wrap" Margin="0,4,0,0" LineHeight="18"/>
                    </StackPanel>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</Page>
